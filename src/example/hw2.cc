#include <bits/stdc++.h>

#include "src/tensor/tensor.h"

#include "src/op/matmul.h"

using NeuroFrame::Tensor, NeuroFrame::dtype_t, NeuroFrame::Device;

int main() {
	{
		// Part 1. Matrix multiplication
		// Here we generate three matrixes. A, B and C, by random
		// And we perform the following iterations again and again:
		// 		Calculate D = A * B
		// 		
	}
}